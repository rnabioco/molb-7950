linters: linters_with_defaults(
    undesirable_function_linter = undesirable_function_linter(
        c("na.omit" = "Use tidyr::drop_na()",
          "gsub" = "Use stringr::str_replace()")
        ),
    T_and_F_symbol_linter = T_and_F_symbol_linter()
    )
exclusions: list(
    "_freeze",
    "_site",
    "renv"
   )
encoding: "UTF-8"
